services:
  postgres-local:
    image: 'postgres:16'
    env_file:
      - .env.local
    ports:
      - '5438:5432'
    volumes:
      - pg-data:/var/lib/postgresql/data

volumes:
  pg-data:
    driver: local
